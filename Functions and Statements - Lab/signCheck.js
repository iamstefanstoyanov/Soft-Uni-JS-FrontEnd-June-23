function signCheck(x, y, z) {
    let res = x * y * z;
    console.log(res > 0 ? "Positive" : "Negative");
}
signCheck(5,
    12,
    -15)
signCheck(-6,
    -12,
    14)
signCheck(-1,
    -2,
    -3)